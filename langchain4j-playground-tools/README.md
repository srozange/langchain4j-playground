# langchain4j-playground-tools

Run the backend:
```bash
mvn quarkus:dev
```

Start the web application:
```shell script
cd ../langchain4j-playground-web
./mvnw quarkus:dev
```

Access the chat UI: http://localhost:8082